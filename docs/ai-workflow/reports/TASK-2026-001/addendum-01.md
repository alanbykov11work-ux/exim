---
report_id: ADD-TASK-2026-001-01
task_id: TASK-2026-001
reported_by: integrator (Alan Bykov) + Claude
reported_at: 2026-09-16T09:40:00Z (UTC)
application_repository: https://github.com/alanbykov11work-ux/exim.git
application_baseline: 8e4612a6ef8c819b08a987cbcf2d862786cefc21
status: blocked
blocked_from: awaiting_application_source
---

# Addendum 01 — состояние работ

Дополнение к `blocker-01.md`. Старый отчёт не переписывается.
Реализация `TASK-2026-001` не начиналась: ветка задачи не создавалась, application code не менялся.

## Что сделано после импорта baseline

| Работа | Состояние | Где |
|---|---|---|
| Импорт application source | Принят в `main` | merge PR #2, `8e4612a` |
| Удаление жёстко прописанных админских email | Готово, ожидает push | ветка `chore/remove-hardcoded-admin-emails`, коммит `a0f879e` |
| Отчёт `blocker-01.md` | Готов, ожидает push | та же ветка |
| Этот addendum | Готов, ожидает push | та же ветка |

Содержание коммита `a0f879e`: из `supabase/admin_roles.sql` убран `UPDATE`, выдававший роль
`admin` трём личным адресам. В публичном репозитории это персональные данные и список целей
для атаки на аккаунты с максимальными правами. Вместо него добавлена `grant_admin(text)` —
первый администратор назначается вручную из SQL Editor и функция закрывается, как только
администратор появился; дальнейшие изменения идут через `admin_set_role()` и попадают в аудит.

## Что остаётся закрытым

1. **Preview.** Репозиторий не подключён к проекту Vercel; `Target environment` не задан.
   Доказать соответствие сборки конкретному commit нечем. Production deploy запрещён и не выполнялся.
2. **Тестовая среда.** Нет non-production Supabase и семи аккаунтов REQ-004.
   Проверки на production-данных запрещены, проверка ролей через UI-переключатель не принимается.
3. **Статус задачи.** `awaiting_application_source`; baseline в `current-task.md` не закреплён.
   Исполнитель канонический статус не меняет — это действие task curator после закрытия пунктов 1 и 2.

## Работа вне задачи: найдена и сохранена

В локальном репозитории интегратора обнаружены изменения, сделанные до появления
координационного каркаса и не проходившие review:

- коммит `f40d969` — hardening RLS, пул полей груза по видам перевозки, оптимизация;
- незакоммиченная Wave 1 — мультитенантность, `workspace_memberships`, переписанные политики,
  `orders_scoped()`, миграция с откатом, фикстуры двух workspace и скрипт создания семи аккаунтов.

Коммит Wave 1 на месте невозможен из-за оставшегося `.git/HEAD.lock`, поэтому работа выгружена
в два патча вне репозитория, чтобы не потеряться. В baseline она **не входит** и внутрь
реализации задачи скрытно перенесена не будет: решение о её судьбе принимает task curator.

## Организационное ограничение

Интегратор работает с двух компьютеров. Доступ к GitHub сохраняется отдельно на каждом, и
подготовленная ветка физически находится на той машине, где велась работа. Из-за этого между
готовым коммитом и его появлением в GitHub возникает задержка: push выполняет человек.
Практическое следствие — незапушенная работа не считается существующей, и любой законченный
кусок отправляется в GitHub сразу, а не накапливается локально.

## Следующее действие

1. Интегратор: push ветки `chore/remove-hardcoded-admin-emails` и PR в `main`.
2. Интегратор: подключить репозиторий к Vercel Preview и завести non-production Supabase.
3. Task curator: закрепить baseline `8e4612a`, перевести `TASK-2026-001` в `ready`.
4. После этого — реализация Foundation Gate волнами, в ветке `task/TASK-2026-001-foundation-gate`.
